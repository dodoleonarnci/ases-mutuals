SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY="public-anon-key"
SUPABASE_SERVICE_ROLE_KEY="service-role-key"

NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="public-anon-key"

# SAML SSO Configuration (Stanford)
# Get these values from Stanford's Service Provider Database (SPDB) after registration
# Documentation: https://uit.stanford.edu/service/saml/onboard-service-provider

# Stanford IdP Configuration
SAML_ENTRYPOINT="https://idp.stanford.edu/"  # Stanford IdP SSO URL (usually https://idp.stanford.edu/)
SAML_IDP_CERT=""  # Stanford's public certificate - download from https://login.stanford.edu/idp.crt

# Your Service Provider Configuration
SAML_ISSUER="https://yourdomain.com/saml"  # Your app's Entity ID (unique identifier, usually a URL)
SAML_ENTITY_ID="https://yourdomain.com/saml"  # Same as SAML_ISSUER, used in metadata
SAML_CALLBACK_URL="https://yourdomain.com/api/auth/saml/callback"  # Where Stanford IdP sends SAML responses
SAML_PUBLIC_CERT=""  # Your app's public certificate (for signing/encryption)
# SAML_PUBLIC_CERT should be in PEM format:
# -----BEGIN CERTIFICATE-----
# ...base64 encoded certificate...
# -----END CERTIFICATE-----

